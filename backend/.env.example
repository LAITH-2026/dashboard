# Copy to .env and adjust. Defaults work out of the box for local dev.

# Port the API listens on
PORT=4000

# Comma-separated allowed CORS origins. Leave empty to allow all (dev default).
# In production set this to your GitHub Pages origin, e.g.:
# CORS_ORIGIN=https://laith-2026.github.io
CORS_ORIGIN=
